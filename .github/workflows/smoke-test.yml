name: Disabled — smoke tests

# This workflow has been disabled per maintainer request (smoke tests temporarily removed).
# To re-enable, restore the original workflow contents or add a new workflow file.

on: {}

jobs: {}
